name: saul
channels:
  - conda-forge
dependencies:
  - esi-core
  - multitaper
  - obspy
  - pandas
  - pip
  - stockwell
  - pip:
      - git+https://github.com/uafgeotools/waveform_collection.git
      - --editable .
